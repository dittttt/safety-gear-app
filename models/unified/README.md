# Drop the unified detector weights here as `best.pt`.
# Optional optimised exports may also live alongside it:
#   best.engine                   (TensorRT, GPU)
#   best.onnx                     (ONNX Runtime)
#   best_openvino_model/          (OpenVINO IR, CPU)
